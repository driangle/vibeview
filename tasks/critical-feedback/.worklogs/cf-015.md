## 2026-08-03T18:20:29Z

Started implementation of the clean-checkout Go test fix.

**Approach:** Track a minimal `internal/spa/dist/index.html` so the `go:embed`
pattern always matches. Production paths remain safe because `make build`, `make
install`, and the GoReleaser hook delete the placeholder directory and copy the
fresh web build before compiling the binary.

**In progress:** Remove CI's manual placeholder setup, document the direct Go
test command, and verify the clean-checkout behavior.

## 2026-08-03T18:24:00Z

Completed the clean-checkout fix.

**Completed:**

- [x] Added a tracked minimal SPA placeholder and narrowly unignored it.
- [x] Removed placeholder creation from test and release validation workflows.
- [x] Documented `cd apps/cli && go test ./...` as the no-prerequisite test path.
- [x] Confirmed release builds remain fresh: `make build`, `make install`, and
  the GoReleaser before hook remove the placeholder and copy `apps/web/dist`.
- [x] Ran `git clean -xdn internal/spa/dist && go test ./...` successfully.

**Open items:** None.
