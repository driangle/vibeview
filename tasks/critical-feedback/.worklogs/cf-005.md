## 2026-08-03T19:49:32Z

Started implementation after confirming that recursive message coverage already
exists and that detection depth is the remaining gap.

**Approach:** Add explicit patterns for common provider credentials and bare
JWTs, followed by conservative entropy-based detection for long hexadecimal and
base64-like blobs. Add realistic multiline tool-output fixtures and document
the best-effort security boundary for LAN users.

## 2026-08-03T19:50:32Z

Completed the redaction improvements. Added explicit standalone detectors for
AWS access-key IDs, OpenAI/Anthropic keys, GitHub tokens, and bare JWTs, plus
entropy-gated detection for long hexadecimal and base64-like credentials.

**Decisions:** Entropy thresholds are deliberately conservative (3.5 bits per
byte for 32+ character hex and 4.3 for 40+ character base64-like strings) to
avoid masking repetitive IDs and placeholders. Provider formats remain explicit
so they are detected independently of entropy.

**Verification:** `go test ./redact/... -v`, `go test ./...` from `apps/lib`,
and `git diff --check` all passed. No open items remain.
