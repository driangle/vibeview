## 2026-08-03T00:00:00Z

Started frontend critical-logic test coverage.

**Approach:** Test the SSE hook through a controllable `EventSource` mock and fake
timers, exercise date helpers at local-day and timezone boundaries, cover the
surviving client-side snippet highlighter, and add focused parser/extractor tests
for malformed and mixed message payloads.

**Initial findings:** Contribution buckets and search ranking/snippet extraction
have moved server-side. The client retains date preset construction and snippet
highlighting. All extractor/parser derivation modules remain client-side.

## 2026-08-03T18:28:41Z

Completed the critical frontend test coverage.

**Completed:**

- [x] Mocked `EventSource` tests for UUID deduplication, 1s/2s reconnect backoff,
  successful reconnect, and unmount cancellation.
- [x] Local-day, timezone-boundary, rolling-day, and date preset tests.
- [x] Case-insensitive, repeated, and literal-character snippet highlighting tests.
- [x] Parser and extractor coverage for mixed, malformed, nested, truncated,
  categorized, and fallback derivations.
- [x] Fixed code-block restoration order in `processMessageContent`; internal-tag-like
  text inside inline or fenced code is no longer removed by catch-all cleanup.

**Verification:** `npm test` passes 87 tests across 15 files. TypeScript and ESLint
also pass through `npm run check-lite`, and `git diff --check` reports no errors.

**Open items:** None.
