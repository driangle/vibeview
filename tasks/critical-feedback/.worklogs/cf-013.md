## 2026-08-03T00:00:00Z

Started coverage of the backend's security-sensitive read and streaming paths.

**Initial findings:** The parser and tailer oversized-line tests were added with
`cf-006`, and the session read endpoint already has an outside-directory symlink
test. Remaining work is subagent endpoint coverage, stream client limits, and a
deterministic test seam for idle decay.

**Approach:** Extend the existing server and broker test suites, reuse real files
and the real tailer/broker path, and extract one heartbeat tick from the timed loop
so idle decay can be tested without a 30-second sleep.

## 2026-08-03T17:34:50Z

Completed critical-path backend coverage.

**Completed:**

- [x] Added HTTP coverage for synthetic `tool_use_` subagent ID resolution;
  confirmed existing happy-path, traversal, and symlink containment coverage.
- [x] Confirmed append-to-tailer-to-broker delivery and added per-session client
  cap coverage.
- [x] Extracted a deterministic heartbeat pass and tested idle decay,
  `activity_state`, and ping delivery.
- [x] Confirmed parser and tailer oversized-line recovery tests from `cf-006`.
- [x] Made the PID ancestry test portable to process sandboxes where `ps` is
  unavailable or the test process is directly parented by init.

**Verification:** `go test -race -cover ./...` passes from `apps/cli`.

**Open items:** None.
