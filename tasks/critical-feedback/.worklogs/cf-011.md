## 2026-08-03T17:40:00Z

Started CF-011 by tracing the live contribution graph, conversation search, file viewer,
and date-range call sites.

**Approach:** Extend existing Go-owned activity and insight responses with render-ready
buckets/operations, make conversation search call the server endpoint in session scope,
and reduce date-range logic to presentation/input behavior.

**Initial findings:** The timeline classifier is isolated to the dormant timeline feature;
the live violations are the activity grid builders, in-conversation search, and file
operation resolver identified by the task.

## 2026-08-03T18:05:00Z

Completed the architecture move.

**Completed:**

- [x] Added UTC-safe Go activity bucket construction and response fields for day, week,
  and month grids; the React components now only calculate visual sizing/tooltips.
- [x] Added server-side conversation message search through session-scoped `/api/search`
  and removed the browser search implementation.
- [x] Added read/write/edit operations to `insights.files`, including read-result cleanup,
  and removed browser tool-result parsing.
- [x] Removed reverse classification of epoch ranges into presets; surviving date logic is
  documented as presentation and user-input shortcuts.
- [x] Added Go tests for activity buckets, message search, and file operations.

**Verification:** `go test ./...` passed in `apps/lib` and `apps/cli`; `npm run
typeCheck && npm run lint` passed in `apps/web`. `make check-lite` remains blocked by
the repository's `golangci-lint` invocation reporting no Go files at the `apps/cli`
module root, while direct module tests pass.

**Open items:** None for CF-011. The dormant timeline classifier remains scoped to the
separate timeline-removal task CF-009.
